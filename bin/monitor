#!/usr/bin/env node

var consul = require('consul')();

setInterval(function() {

  consul.health.state(function(err, result) {
    if (err) {
      return console.error(err);
    }
    console.log(result);
  });

}, 1000);
